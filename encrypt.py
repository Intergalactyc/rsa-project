import sys
# argparse?


# Encrypt a message using a public key

def encrypt(message: str, key: tuple[int,int]) -> str:
    pass

if __name__ == "__main__:
    # if a file is passed in, encrypt it (line by line? overall? depend on length???)
    # if a string is passed in, encrypt it
